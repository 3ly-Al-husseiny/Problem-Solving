/* Write your T-SQL query statement below */

-- using joins , Not the best for the performance 
select customer_id 
from customer 
group by customer_id
having count(distinct product_key) = 
(
    select count(distinct product_key)
    from product
);